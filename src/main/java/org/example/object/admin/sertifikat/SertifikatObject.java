package org.example.object.admin.sertifikat;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;

public class SertifikatObject {
    WebDriver driver;

    public SertifikatObject(WebDriver driver) {
        this.driver = driver;
    }
}
